package KafkaTesting.demo.Test.Controller;import KafkaTesting.demo.Test.Consumer.KafkaConsumer;import KafkaTesting.demo.Test.Model.UserEvent;import KafkaTesting.demo.Test.Producer.KafkaProducer;import KafkaTesting.demo.Test.Service.WikiMediaService;import org.springframework.web.bind.annotation.*;import java.util.List;@RestController@RequestMapping("/testing")public class KafkaController {    private final KafkaProducer producer;    private final KafkaConsumer Cosumer;    public KafkaController(KafkaProducer producer, KafkaConsumer cosumer) {        this.producer = producer;        Cosumer = cosumer;    }    @PostMapping()    public String send(@RequestBody UserEvent userEvent) {        producer.sendEvent(userEvent.getUserId(),userEvent); // userId is key for partitioning        return "Event sent to Kafka";    }    @GetMapping    public List<UserEvent> send() {       return Cosumer.getAllEvents();    }    @GetMapping("/wikimedia")    public void triggerHandler(){        try {            new WikiMediaService().implementServiceLogic();        } catch (InterruptedException e) {            throw new RuntimeException(e);        }    }    @GetMapping("/getWikiMediaData")    public void datareceiver(){        try {            new WikiMediaService().implementServiceLogic();        } catch (InterruptedException e) {            throw new RuntimeException(e);        }    }}